<server description="Simple dev server">
  <featureManager>
    <feature>servlet-4.0</feature>
  </featureManager>

  <httpEndpoint id="defaultHttpEndpoint"
                httpPort="9080"
                httpsPort="9443"
                host="*"/>

  <!-- If you place WAR in dropins/ it will be picked up automatically -->
</server>